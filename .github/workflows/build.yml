name: CI Test Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run echo
        run: echo "Hello from GitHub Actions!"
